name: 'WAS Deployment CD Pipeline UAT'
on:
  workflow_call:
    inputs:
      application:
        description: 'Application name for deployment'
        required: true
        type: string
      oc:
        description: 'OC number'
        required: true
        type: string
      environment:
        description: 'Environment to deploy to'
        required: true
        type: string
        default: 'uat'
      site:
        description: 'Site to deploy to'
        required: true
        type: string
        default: 'site1'
  workflow_dispatch:
    inputs:
      application:
        description: 'Application name for deployment'
        required: true
        type: string
      oc:
        description: 'OC number'
        required: true
        type: string
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        default: 'uat'
        options:
          - dev 
          - uat
          - sit
          - prod
      site:
        description: 'Site to deploy to'
        required: true
        type: choice
        default: 'site1'
        options:
          - site1
          - site2

jobs:
  prepare:
    name: 'Prepare Deployment'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      - name: 'Artifactory Login'
        id: artifactory-login

        run: |
          echo "Logging into Artifactory for ${{ github.event.inputs.application }}"
          # Add your commands to log into Artifactory here  
        # uses: ibkpe-platform-org/dso-was-reusable/.github/actions/artifactory-login
        # uses: ./github/actions/artifactory-login                
        # with:
        #   application: ${{ steps.prepare.outputs.lower-application }}
        #   artifactory-username: ${{ env.ARTIFACTORY_USER_NAME }}
        # env:
        #   ARTIFACTORY_USER_PASSWORD: ${{ secrets.ARTIFACTORY_TOKEN }}
        
      - name: Download build artifacts from Artifactory
        run: |
          echo "Downloading build artifacts for ${{ github.event.inputs.application }} from Artifactory"
          # Add your commands to download artifacts here
        continue-on-error: false

      - name: Generate Evidence Summary
        run: |
          echo "Generating evidence summary for ${{ github.event.inputs.application }} deployment"
          # Add your commands to generate evidence summary here
    
  backup:
    name: 'Backup Current Deployment'
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      - name: 'Backup Current Deployment'
        run: |
          echo "Backing up current deployment of ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          # Add your backup commands here

  copy_artifacts:
    name: 'Copy Artifacts to Deployment Server'
    needs: backup
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      - name: 'Copy Artifacts'
        run: |
          echo "Copying artifacts for ${{ github.event.inputs.application }} to deployment server"
          # Add your commands to copy artifacts here

  deploy_site:
    name: 'Deploy to Site'
    needs: copy_artifacts
    # environment:
    #   name: deploy_site1
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      - name: 'Deploy Application to Site'
        run: |
          echo "Deploying ${{ github.event.inputs.application }} to Site ${{ github.event.inputs.site }} in ${{ github.event.inputs.environment }}"
          # Add your deployment commands for Site ${{ github.event.inputs.site }} here

  post_deployment:
    name: 'Post Deployment Verification'
    needs: deploy_site
    # environment:
    #   name: Validation
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      - name: 'Verify Deployment'
        run: |
          echo "::notice::Verifying deployment of ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          echo "Verifying deployment of ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          # Add your verification commands here
      
      # Add failure to trigger rollback (for testing purposes)
      - name: 'Simulate Failure for Rollback'
        run: |
          echo "::error::Simulating failure to trigger rollback"
          exit 1

  resumen:
    name: 'Generate Deployment Summary'
    needs: post_deployment
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      - name: 'Generate Summary'
        run: |
          echo "::notice::Generating deployment summary for ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          echo "Generating deployment summary for ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          # Add your commands to generate a summary here
      
      - name: 'Upload Summary'
        # uses: actions/upload-artifact@v3
        run: |
          echo "Uploading deployment summary for ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          # Add your commands to upload the summary artifact here

  rollback:
    name: 'Rollback Deployment'
    needs: [deploy_site, post_deployment]
    runs-on: ubuntu-latest
    if: |
      (needs.deploy_site.result == 'success') &&
      (needs.post_deployment.result == 'failure' || needs.post_deployment.result == 'cancelled')
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v5

      # Call another workflow for rollback
      - name: 'Trigger Rollback Workflow'
        uses: actions/github-script@v6
        with:
          script: |
            const { owner, repo } = context.repo;
            const workflow_id = '.github/workflows/_cd-rollback-prv.yml';
            const ref = context.ref;
            const inputs = {
              application: '${{ github.event.inputs.application }}',
              environment: '${{ github.event.inputs.environment }}',
              oc: '${{ github.event.inputs.oc }}'
            };
            await github.actions.createWorkflowDispatch({
              owner,
              repo,
              workflow_id,
              ref,
              inputs
            });
    
      - name: 'Rollback Deployment'
        run: |
          echo "::notice::Rolling back deployment of ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          echo "Rolling back deployment of ${{ github.event.inputs.application }} in ${{ github.event.inputs.environment }}"
          # Add your rollback commands here
        
